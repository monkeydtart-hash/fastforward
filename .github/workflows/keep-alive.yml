name: Keep Render app awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Fast Forward
        run: curl -sS -o /dev/null -w "%{http_code}\n" https://fastforward-2ksy.onrender.com
